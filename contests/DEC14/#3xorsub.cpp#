#include <bits/stdc++.h>

using namespace std;

bool lkp[1010];

long long calc_max(long long *arr,long long n,long long xorv){
  long long temp;
  if(n==1)
    return lkp[arr[n]]=true;
  else{
    return calc_max(arr,n-1,xorv)
  }
}

int main(){
  long long t,n,k,i;
  long long arr[1001];
  scanf("%lld",&t);
  while(t--){
    scanf("%lld",&n);
    scanf("%lld",&k);
    for(i=0;i<1010;i++)
      lkp[i]=false;
    for(i=0;i<n;i++)
      scanf("%lld",&arr[i]);
    xorv=0;
    calc_max(arr,n,xorv);
  }
  return 0;
}
