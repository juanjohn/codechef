#include <iostream>
#include <cmath>

using namespace std;

int main(){
  int n;
  cin>>n;
  char cvote,pvote;
  int cscore,pscore;
  cin>>cvote>>score;
  pvote=cvote;
  int minusers=abs(vote)-1;
  for(int i=0;i<n;i++){
    cin>>cvote>>cscore;
  }
  return 0;
}
