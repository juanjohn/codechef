#include <stdio.h>

int main(){
  int n,k,i,count=0;
  scanf("%lld%lld",&n,&k);
  int arr[n];
  for(i=0;i<n;i++){
    scanf("%lld",&arr[i]);
    if(arr[i]%k==0)
      count++;
  }
  printf("%lld\n",count);  
  return 0;
}
